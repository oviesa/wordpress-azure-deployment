# wordpress-azure-deployment
A WordPress website deployed on an Ubuntu Azure VM using Apache, MariaDB &amp; PHP. Configured a Linux server environment, database integration, &amp; security plugins to support a production-style web deployment.
